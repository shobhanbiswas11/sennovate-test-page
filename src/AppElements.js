import styled from "styled-components";
import { Header as HeaderComp } from "./components";
import { ContactPage as ContactPageComp } from "./containers";

export const Header = styled(HeaderComp)`
  height: 10vh;
  padding: 0 2rem;
`;

export const ContactPage = styled(ContactPageComp)``;
